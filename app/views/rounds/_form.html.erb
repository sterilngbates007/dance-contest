<%= simple_form_for(@round) do |f| %>
	<%= f.association :contest %>
	<%= f.association :contestant %>
	<%= f.association :judge %>
	<%= f.input :cycle, :label => "Round Cycle"%>
	<%= f.input :place, :label => "Placement byJudge" %>
	<%= @round.name %><br />
	<%= f.button :submit %>
<% end %>

