<h1>Doing Placement for a Round</h1>
	<%= Round.find_all_by_contest_id(@contest) %>
<br />
<br />
	<%= @contest.judges %>
<br />
<br />
<%= simple_form_for(@contest) do |f| %>
<%= f.input :name %>

	<% @contest.judges.each do |highest| %>
		<%= highest.name %> <br />	
			
			<% @contest.contestants.each do |lowest| %>
				&nbsp; <%= lowest.name %>
					
				<%= %>
				<br />
			<% end %>
	<% end %>
<br />
<br />

<%= f.button :submit %>
<% end %> 
<br />
<%= link_to 'Show', @contest %> |
<%= link_to 'Back', contests_path %>
